<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme" parent="@android:style/Theme.Material.Light"></style>

    <style name="ChannelListTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:textColor">@android:color/white</item>

        <!--  It should be true otherwise action mode will not overlay toolbar -->
        <item name="windowActionModeOverlay">true</item>
    </style>

    <style name="ListViews" parent="@android:style/Widget.ListView">
        <item name="android:divider">#414141</item>
        <item name="android:dividerHeight">1px</item>
    </style>
</resources>